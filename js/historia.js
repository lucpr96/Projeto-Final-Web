var button = document.getElementById('read_button');

button.addEventListener('click', function() {
    var card = document.querySelector('.card');
    card.classList.toggle('active');

    if (card.classList.contains('active')) {
        
    }

    button.textContent = 'Read more'

});


var button = document.getElementById('read');

button.addEventListener('click', function() {
    var card = document.querySelector('.card1');
    card.classList.toggle('active');

    if (card.classList.contains('active')) {
        
    }

    button.textContent = 'Read more'

});


var button = document.getElementById('read_');

button.addEventListener('click', function() {
    var card = document.querySelector('.card2');
    card.classList.toggle('active');

    if (card.classList.contains('active')) {
        
    }

    button.textContent = 'Read more'

});


var button = document.getElementById('read_b');

button.addEventListener('click', function() {
    var card = document.querySelector('.card3');
    card.classList.toggle('active');

    if (card.classList.contains('active')) {
        
    }

    button.textContent = 'Read more'

});


var button = document.getElementById('read_bu');

button.addEventListener('click', function() {
    var card = document.querySelector('.card4');
    card.classList.toggle('active');

    if (card.classList.contains('active')) {
        
    }

    button.textContent = 'Read more'

});


var button = document.getElementById('read_but');

button.addEventListener('click', function() {
    var card = document.querySelector('.card5');
    card.classList.toggle('active');

    if (card.classList.contains('active')) {
        
    }

    button.textContent = 'Read more'

});


var button = document.getElementById('read_butt');

button.addEventListener('click', function() {
    var card = document.querySelector('.card6');
    card.classList.toggle('active');

    if (card.classList.contains('active')) {
        
    }

    button.textContent = 'Read more'

});

const hamburger = document.querySelector(".hamburger");
const nav = document.querySelector(".nav");

hamburger.addEventListener("click", () => nav.classList.toggle("active"));